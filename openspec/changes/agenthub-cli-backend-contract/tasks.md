# Tasks

- [x] Add Spectra spec delta for AgentHub backend CLI operations.
- [x] Add unit tests for worktree layout validation and Git porcelain parsing.
- [x] Add integration tests for project/worktree handlers against a temporary Git repository.
- [x] Add CLI end-to-end tests for `project` and `worktree` JSON contracts.
- [x] Implement a controlled Git command runner with timeouts and structured outcomes.
- [x] Implement AgentHub project/worktree services.
- [x] Add `agentbridge project` and `agentbridge worktree` commands.
- [x] Run `bun run check`.
- [ ] Run full `bun run test` without environment-native dependency failures.
